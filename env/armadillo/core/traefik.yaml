---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik
spec:
  values:
    gateway:
      listeners:
        web:
          hostname: "*.armadillo.com"
        websecure:
          port: 8443
          protocol: HTTPS
          hostname: "*.armadillo.com"
          namespacePolicy:
            from: All
          mode: Terminate
          certificateRefs:
            - name: wildcard-armadillo-local-tls
              namespace: traefik

---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-extension
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./env/javelina/infrastructure-extension
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: infrastructure
